# TargetLibraries_sctsim

To install the TargetLibraries on your system run the bash scripts in each respective folder called rebuildThisTargetLibrary.sh using this command 

./rebuildThisTargetLibrary.sh

IN THIS ORDER ONLY

1. Target Driver
2. Target IO
3. Target Calib

